import type { Drill, Skill } from "@/types/drill";

const makeDrill = (
  id: string,
  title: string,
  skill: Skill,
  prompt: string,
  starterCode: string,
  tests: string,
  referenceSolution: string,
  explanation: string,
  targetTimeSeconds = 120,
  tags: string[] = [],
): Drill => ({
  id,
  title,
  skill,
  difficulty: targetTimeSeconds > 150 ? "intermediate" : "fundamental",
  prompt,
  targetTimeSeconds,
  starterCode: starterCode.trim(),
  testCode: tests.trim(),
  referenceSolution: referenceSolution.trim(),
  explanation,
  hints: [
    "Name the invariant before writing the loop.",
    "Initialize the data structure before the main pass.",
    "Check the smallest input that should work.",
  ],
  expectedTimeComplexity: "O(n)",
  expectedSpaceComplexity: "O(n)",
  tags,
});

const simpleTests = (fn: string, assertions: string[]) => `
TESTS = [
${assertions
  .map(
    (assertion, index) => `    ("case ${index + 1}", lambda: (${assertion}))`,
  )
  .join(",\n")}
]
`;

export const drills: Drill[] = [
  makeDrill("py-frequency-dict", "Frequency Dictionary", "python", "Return a dictionary mapping each value to its frequency.", "def frequency(items):\n    pass", simpleTests("frequency", ["frequency(['a','b','a']) == {'a': 2, 'b': 1}", "frequency([]) == {}", "frequency([1,1,2,3,3,3]) == {1: 2, 2: 1, 3: 3}"]), "def frequency(items):\n    counts = {}\n    for item in items:\n        counts[item] = counts.get(item, 0) + 1\n    return counts", "Dictionary counting should feel automatic: initialize missing keys and increment once per item.", 90),
  makeDrill("py-counter", "Counter Basics", "python", "Return the two most common values using collections.Counter.", "from collections import Counter\n\ndef two_most_common(items):\n    pass", simpleTests("two_most_common", ["two_most_common(['a','b','a','c','b','a']) == [('a', 3), ('b', 2)]", "two_most_common([1]) == [(1, 1)]"]), "from collections import Counter\n\ndef two_most_common(items):\n    return Counter(items).most_common(2)", "Counter is the fastest way to express common interview frequency tasks.", 60),
  makeDrill("py-defaultdict-groups", "Group With Defaultdict", "python", "Group words by their first letter.", "from collections import defaultdict\n\ndef group_by_first(words):\n    pass", simpleTests("group_by_first", ["group_by_first(['bat','ball','cat']) == {'b': ['bat', 'ball'], 'c': ['cat']}", "group_by_first([]) == {}"]), "from collections import defaultdict\n\ndef group_by_first(words):\n    groups = defaultdict(list)\n    for word in words:\n        groups[word[0]].append(word)\n    return dict(groups)", "defaultdict(list) removes the repeated key-exists branch.", 90),
  makeDrill("py-deque-queue", "Deque Queue", "python", "Consume a list in FIFO order using deque and return the popped values.", "from collections import deque\n\ndef drain_queue(items):\n    pass", simpleTests("drain_queue", ["drain_queue([1,2,3]) == [1,2,3]", "drain_queue([]) == []"]), "from collections import deque\n\ndef drain_queue(items):\n    q = deque(items)\n    out = []\n    while q:\n        out.append(q.popleft())\n    return out", "BFS muscle memory starts with deque and popleft.", 75),
  makeDrill("py-heap-push-pop", "Heap Push Pop", "heap", "Push every value into a min heap and return values in sorted order.", "import heapq\n\ndef heap_sort_values(nums):\n    pass", simpleTests("heap_sort_values", ["heap_sort_values([3,1,2]) == [1,2,3]", "heap_sort_values([]) == []", "heap_sort_values([5,-1,5]) == [-1,5,5]"]), "import heapq\n\ndef heap_sort_values(nums):\n    heap = []\n    for num in nums:\n        heapq.heappush(heap, num)\n    return [heapq.heappop(heap) for _ in range(len(heap))]", "Python heaps are min-heaps; push, then pop the smallest.", 120),
  makeDrill("py-sort-key", "Sort With Key", "python", "Sort intervals by their end value.", "def sort_by_end(intervals):\n    pass", simpleTests("sort_by_end", ["sort_by_end([[3,4],[1,2],[2,3]]) == [[1,2],[2,3],[3,4]]", "sort_by_end([[5,1],[2,0]]) == [[2,0],[5,1]]"]), "def sort_by_end(intervals):\n    return sorted(intervals, key=lambda interval: interval[1])", "Lambda keys are a small syntax detail that should not cost interview time.", 60),
  makeDrill("py-set-dedupe", "Set Deduplication", "python", "Return unique values preserving first-seen order.", "def dedupe(nums):\n    pass", simpleTests("dedupe", ["dedupe([3,1,3,2,1]) == [3,1,2]", "dedupe([]) == []"]), "def dedupe(nums):\n    seen = set()\n    out = []\n    for num in nums:\n        if num not in seen:\n            seen.add(num)\n            out.append(num)\n    return out", "Use a set for membership and a list for output order.", 90),
  makeDrill("hash-index-map", "Value Index Map", "hashing", "Return a value to index map for the input list.", "def index_map(nums):\n    pass", simpleTests("index_map", ["index_map([4,7,9]) == {4:0, 7:1, 9:2}", "index_map([]) == {}"]), "def index_map(nums):\n    return {value: i for i, value in enumerate(nums)}", "Index maps are the base primitive under two-sum style problems.", 75),
  makeDrill("hash-count-frequencies", "Count Frequencies", "hashing", "Return True if any value appears more than once.", "def contains_duplicate(nums):\n    pass", simpleTests("contains_duplicate", ["contains_duplicate([1,2,1]) is True", "contains_duplicate([1,2,3]) is False"]), "def contains_duplicate(nums):\n    seen = set()\n    for num in nums:\n        if num in seen:\n            return True\n        seen.add(num)\n    return False", "Most hashing drills reduce to a constant-time seen check.", 90),
  makeDrill("graph-adjacency-list", "Build Adjacency List", "graph", "Given directed edges, return an adjacency list.", "from collections import defaultdict\n\ndef build_graph(edges):\n    pass", simpleTests("build_graph", ["build_graph([(1,2),(1,3),(2,3)]) == {1:[2,3], 2:[3]}", "build_graph([]) == {}"]), "from collections import defaultdict\n\ndef build_graph(edges):\n    graph = defaultdict(list)\n    for src, dst in edges:\n        graph[src].append(dst)\n    return dict(graph)", "Graph setup should be mechanical before traversal starts.", 100),
  makeDrill("two-pointer-opposite", "Opposite-End Pointers", "two-pointers", "Return True if a sorted list has two numbers summing to target.", "def two_sum_sorted(nums, target):\n    pass", simpleTests("two_sum_sorted", ["two_sum_sorted([1,2,4,7], 6) is True", "two_sum_sorted([1,2,4], 8) is False"]), "def two_sum_sorted(nums, target):\n    left, right = 0, len(nums) - 1\n    while left < right:\n        total = nums[left] + nums[right]\n        if total == target:\n            return True\n        if total < target:\n            left += 1\n        else:\n            right -= 1\n    return False", "Sorted arrays let pointer movement discard impossible pairs.", 120),
  makeDrill("two-pointer-write", "Read Write Pointer", "two-pointers", "Move nonzero values forward in-place and return the compacted prefix length.", "def compact_nonzero(nums):\n    pass", simpleTests("compact_nonzero", ["(lambda a: (compact_nonzero(a), a[:3]))([0,1,0,2,3]) == (3, [1,2,3])", "(lambda a: (compact_nonzero(a), a[:0]))([0,0]) == (0, [])"]), "def compact_nonzero(nums):\n    write = 0\n    for read in range(len(nums)):\n        if nums[read] != 0:\n            nums[write] = nums[read]\n            write += 1\n    return write", "Read scans every item; write marks the next kept slot.", 120),
  makeDrill("window-fixed-sum", "Fixed Sliding Window", "sliding-window", "Return the maximum sum of any window of size k.", "def max_window_sum(nums, k):\n    pass", simpleTests("max_window_sum", ["max_window_sum([1,4,2,10], 2) == 12", "max_window_sum([5], 1) == 5"]), "def max_window_sum(nums, k):\n    current = sum(nums[:k])\n    best = current\n    for right in range(k, len(nums)):\n        current += nums[right] - nums[right - k]\n        best = max(best, current)\n    return best", "Fixed windows add the new right value and remove the value that fell out.", 120),
  makeDrill("window-variable", "Variable Sliding Window", "sliding-window", "Return the minimum length subarray with sum at least target, or 0.", "def min_subarray_len(target, nums):\n    pass", simpleTests("min_subarray_len", ["min_subarray_len(7, [2,3,1,2,4,3]) == 2", "min_subarray_len(10, [1,2,3]) == 0"]), "def min_subarray_len(target, nums):\n    left = 0\n    total = 0\n    best = float('inf')\n    for right, value in enumerate(nums):\n        total += value\n        while total >= target:\n            best = min(best, right - left + 1)\n            total -= nums[left]\n            left += 1\n    return 0 if best == float('inf') else best", "Expand right until valid; shrink left while the invariant remains valid.", 150),
  makeDrill("window-frequency", "Window Frequency Map", "sliding-window", "Return True if any length-k window contains duplicate values.", "def has_duplicate_window(nums, k):\n    pass", simpleTests("has_duplicate_window", ["has_duplicate_window([1,2,3,1], 3) is False", "has_duplicate_window([1,2,3,1], 4) is True"]), "def has_duplicate_window(nums, k):\n    counts = {}\n    for right, num in enumerate(nums):\n        counts[num] = counts.get(num, 0) + 1\n        if right >= k:\n            old = nums[right - k]\n            counts[old] -= 1\n            if counts[old] == 0:\n                del counts[old]\n        if counts[num] > 1:\n            return True\n    return False", "Frequency maps let a window track more than just sums.", 150),
  makeDrill("binary-search-exact", "Binary Search", "binary-search", "Return the index of target in a sorted list, or -1.", "def binary_search(nums, target):\n    pass", simpleTests("binary_search", ["binary_search([1,3,5], 3) == 1", "binary_search([1,3,5], 2) == -1", "binary_search([], 1) == -1"]), "def binary_search(nums, target):\n    left, right = 0, len(nums) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            return mid\n        if nums[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1", "Classic binary search is a boundary update drill.", 120),
  makeDrill("binary-search-lower", "Lower Bound", "binary-search", "Return the first index whose value is at least target.", "def lower_bound(nums, target):\n    pass", simpleTests("lower_bound", ["lower_bound([1,3,3,5], 3) == 1", "lower_bound([1,2], 5) == 2", "lower_bound([], 1) == 0"]), "def lower_bound(nums, target):\n    left, right = 0, len(nums)\n    while left < right:\n        mid = (left + right) // 2\n        if nums[mid] < target:\n            left = mid + 1\n        else:\n            right = mid\n    return left", "Lower-bound keeps the answer inside [left, right).", 150),
  makeDrill("binary-search-answer", "Binary Search Answer", "binary-search", "Given sorted piles and hours, return the minimum eating speed.", "import math\n\ndef min_speed(piles, hours):\n    pass", simpleTests("min_speed", ["min_speed([3,6,7,11], 8) == 4", "min_speed([30,11,23,4,20], 5) == 30"]), "import math\n\ndef min_speed(piles, hours):\n    left, right = 1, max(piles)\n    while left < right:\n        mid = (left + right) // 2\n        needed = sum(math.ceil(p / mid) for p in piles)\n        if needed <= hours:\n            right = mid\n        else:\n            left = mid + 1\n    return left", "Binary search over answers needs a monotonic feasibility check.", 180),
  makeDrill("list-reverse", "Reverse Linked List", "linked-list", "Reverse a singly linked list and return the new head.", "def reverse_list(head):\n    pass", "class Node:\n    def __init__(self, val, next=None):\n        self.val = val\n        self.next = next\n\ndef values(head):\n    out = []\n    while head:\n        out.append(head.val)\n        head = head.next\n    return out\n\na = Node(1, Node(2, Node(3)))\nTESTS = [(\"reverses\", lambda: values(reverse_list(a)) == [3,2,1]), (\"empty\", lambda: reverse_list(None) is None)]", "def reverse_list(head):\n    prev = None\n    current = head\n    while current:\n        nxt = current.next\n        current.next = prev\n        prev = current\n        current = nxt\n    return prev", "Keep previous, current, and next straight; update the link before advancing.", 150),
  makeDrill("list-middle", "Middle Node", "linked-list", "Return the middle node of a linked list.", "def middle_node(head):\n    pass", "class Node:\n    def __init__(self, val, next=None):\n        self.val = val\n        self.next = next\n\na = Node(1, Node(2, Node(3)))\nb = Node(1, Node(2, Node(3, Node(4))))\nTESTS = [(\"odd\", lambda: middle_node(a).val == 2), (\"even second middle\", lambda: middle_node(b).val == 3)]", "def middle_node(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n    return slow", "Fast/slow pointers encode half-speed traversal.", 120),
  makeDrill("list-cycle", "Linked List Cycle", "linked-list", "Return True if the linked list has a cycle.", "def has_cycle(head):\n    pass", "class Node:\n    def __init__(self, val):\n        self.val = val\n        self.next = None\n\na = Node(1); b = Node(2); c = Node(3); a.next = b; b.next = c; c.next = b\nx = Node(1); x.next = Node(2)\nTESTS = [(\"cycle\", lambda: has_cycle(a) is True), (\"no cycle\", lambda: has_cycle(x) is False)]", "def has_cycle(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow is fast:\n            return True\n    return False", "If fast laps slow, a cycle exists.", 130),
  makeDrill("list-merge", "Merge Sorted Lists", "linked-list", "Merge two sorted linked lists and return the head.", "def merge_lists(a, b):\n    pass", "class Node:\n    def __init__(self, val, next=None):\n        self.val = val\n        self.next = next\n\ndef vals(h):\n    out=[]\n    while h:\n        out.append(h.val); h=h.next\n    return out\n\na = Node(1, Node(3)); b = Node(2, Node(4))\nTESTS = [(\"merge\", lambda: vals(merge_lists(a,b)) == [1,2,3,4]), (\"empty\", lambda: vals(merge_lists(None, Node(5))) == [5])]", "def merge_lists(a, b):\n    dummy = tail = type('Node', (), {})()\n    while a and b:\n        if a.val <= b.val:\n            tail.next = a\n            a = a.next\n        else:\n            tail.next = b\n            b = b.next\n        tail = tail.next\n    tail.next = a or b\n    return dummy.next", "A dummy head removes the special case for the first appended node.", 180),
  makeDrill("tree-preorder", "Tree Preorder", "tree", "Return preorder traversal values.", "def preorder(root):\n    pass", "class Node:\n    def __init__(self, val, left=None, right=None):\n        self.val=val; self.left=left; self.right=right\nroot=Node(1, Node(2), Node(3))\nTESTS=[('preorder', lambda: preorder(root)==[1,2,3]), ('empty', lambda: preorder(None)==[])]", "def preorder(root):\n    if not root:\n        return []\n    return [root.val] + preorder(root.left) + preorder(root.right)", "Preorder is node, left, right.", 90),
  makeDrill("tree-inorder", "Tree Inorder", "tree", "Return inorder traversal values.", "def inorder(root):\n    pass", "class Node:\n    def __init__(self, val, left=None, right=None):\n        self.val=val; self.left=left; self.right=right\nroot=Node(2, Node(1), Node(3))\nTESTS=[('inorder', lambda: inorder(root)==[1,2,3]), ('empty', lambda: inorder(None)==[])]", "def inorder(root):\n    if not root:\n        return []\n    return inorder(root.left) + [root.val] + inorder(root.right)", "Inorder is left, node, right.", 90),
  makeDrill("tree-depth", "Tree Max Depth", "dfs", "Return the maximum depth of a binary tree.", "def max_depth(root):\n    pass", "class Node:\n    def __init__(self, val, left=None, right=None):\n        self.val=val; self.left=left; self.right=right\nroot=Node(1, Node(2, Node(4)), Node(3))\nTESTS=[('depth', lambda: max_depth(root)==3), ('empty', lambda: max_depth(None)==0)]", "def max_depth(root):\n    if not root:\n        return 0\n    return 1 + max(max_depth(root.left), max_depth(root.right))", "Tree DFS often combines child answers into a parent answer.", 100),
  makeDrill("tree-level-order", "Tree Level Order", "bfs", "Return binary tree values level by level.", "from collections import deque\n\ndef level_order(root):\n    pass", "class Node:\n    def __init__(self, val, left=None, right=None):\n        self.val=val; self.left=left; self.right=right\nroot=Node(1, Node(2), Node(3))\nTESTS=[('levels', lambda: level_order(root)==[[1],[2,3]]), ('empty', lambda: level_order(None)==[])]", "from collections import deque\n\ndef level_order(root):\n    if not root:\n        return []\n    q = deque([root])\n    levels = []\n    while q:\n        level = []\n        for _ in range(len(q)):\n            node = q.popleft()\n            level.append(node.val)\n            if node.left: q.append(node.left)\n            if node.right: q.append(node.right)\n        levels.append(level)\n    return levels", "The current queue length defines one level.", 150),
  makeDrill("graph-dfs-recursive", "Recursive Graph DFS", "dfs", "Return nodes visited by recursive DFS from start.", "def dfs_order(graph, start):\n    pass", simpleTests("dfs_order", ["dfs_order({'A':['B','C'], 'B':[], 'C':[]}, 'A') == ['A','B','C']", "dfs_order({1:[2],2:[1]}, 1) == [1,2]"]), "def dfs_order(graph, start):\n    seen = set()\n    out = []\n    def visit(node):\n        if node in seen:\n            return\n        seen.add(node)\n        out.append(node)\n        for nei in graph.get(node, []):\n            visit(nei)\n    visit(start)\n    return out", "Mark before recursing to survive cycles.", 120),
  makeDrill("graph-dfs-iterative", "Iterative Graph DFS", "dfs", "Return DFS order using an explicit stack.", "def iterative_dfs(graph, start):\n    pass", simpleTests("iterative_dfs", ["iterative_dfs({'A':['B','C'], 'B':[], 'C':[]}, 'A') == ['A','B','C']", "iterative_dfs({1:[2],2:[1]}, 1) == [1,2]"]), "def iterative_dfs(graph, start):\n    stack = [start]\n    seen = set()\n    out = []\n    while stack:\n        node = stack.pop()\n        if node in seen:\n            continue\n        seen.add(node)\n        out.append(node)\n        for nei in reversed(graph.get(node, [])):\n            stack.append(nei)\n    return out", "A stack mirrors recursive DFS call order.", 130),
  makeDrill("graph-bfs-basic", "Graph BFS", "bfs", "Implement breadth-first search over an adjacency-list graph. Return nodes in traversal order and do not visit nodes twice.", "from collections import deque\n\ndef bfs(graph, start):\n    pass", simpleTests("bfs", ["bfs({'A':['B','C'], 'B':['D'], 'C':[], 'D':[]}, 'A') == ['A','B','C','D']", "bfs({1:[2], 2:[1]}, 1) == [1,2]", "bfs({'A': []}, 'A') == ['A']"]), "from collections import deque\n\ndef bfs(graph, start):\n    queue = deque([start])\n    visited = {start}\n    result = []\n    while queue:\n        node = queue.popleft()\n        result.append(node)\n        for neighbor in graph.get(node, []):\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append(neighbor)\n    return result", "BFS processes nodes by increasing distance using FIFO order. Mark visited when enqueueing.", 120),
  makeDrill("grid-dfs", "Grid DFS", "dfs", "Count connected 1-cells in a grid from a starting cell.", "def island_size(grid, row, col):\n    pass", simpleTests("island_size", ["island_size([[1,1,0],[0,1,0]], 0, 0) == 3", "island_size([[0]], 0, 0) == 0"]), "def island_size(grid, row, col):\n    rows, cols = len(grid), len(grid[0])\n    seen = set()\n    def dfs(r, c):\n        if r < 0 or c < 0 or r == rows or c == cols or grid[r][c] == 0 or (r, c) in seen:\n            return 0\n        seen.add((r, c))\n        return 1 + dfs(r+1,c) + dfs(r-1,c) + dfs(r,c+1) + dfs(r,c-1)\n    return dfs(row, col)", "Grid DFS is graph DFS with bounds checks.", 150),
  makeDrill("grid-bfs", "Grid BFS", "bfs", "Return shortest path length from S to T in a grid with # as walls.", "from collections import deque\n\ndef shortest_grid(grid):\n    pass", simpleTests("shortest_grid", ["shortest_grid(['S..', '##.', '..T']) == 4", "shortest_grid(['ST']) == 1", "shortest_grid(['S#', '#T']) == -1"]), "from collections import deque\n\ndef shortest_grid(grid):\n    rows, cols = len(grid), len(grid[0])\n    start = None\n    for r in range(rows):\n        for c in range(cols):\n            if grid[r][c] == 'S':\n                start = (r, c)\n    q = deque([(start[0], start[1], 0)])\n    seen = {start}\n    while q:\n        r, c, dist = q.popleft()\n        if grid[r][c] == 'T':\n            return dist\n        for dr, dc in [(1,0),(-1,0),(0,1),(0,-1)]:\n            nr, nc = r + dr, c + dc\n            if 0 <= nr < rows and 0 <= nc < cols and grid[nr][nc] != '#' and (nr, nc) not in seen:\n                seen.add((nr, nc)); q.append((nr, nc, dist + 1))\n    return -1", "BFS gives shortest paths in unweighted grids.", 180),
  makeDrill("topo-kahn", "Kahn Topological Sort", "topological-sort", "Return a topological ordering for num_courses and prerequisite edges.", "from collections import defaultdict, deque\n\ndef topo_order(num_courses, prerequisites):\n    pass", simpleTests("topo_order", ["topo_order(3, [(1,0),(2,1)]) == [0,1,2]", "topo_order(2, [(0,1),(1,0)]) == []"]), "from collections import defaultdict, deque\n\ndef topo_order(num_courses, prerequisites):\n    graph = defaultdict(list)\n    indegree = [0] * num_courses\n    for course, pre in prerequisites:\n        graph[pre].append(course)\n        indegree[course] += 1\n    q = deque([i for i, deg in enumerate(indegree) if deg == 0])\n    order = []\n    while q:\n        node = q.popleft()\n        order.append(node)\n        for nei in graph[node]:\n            indegree[nei] -= 1\n            if indegree[nei] == 0:\n                q.append(nei)\n    return order if len(order) == num_courses else []", "Kahn's algorithm repeatedly removes zero-indegree nodes and detects cycles by processed count.", 180),
  makeDrill("topo-dfs-cycle", "DFS Cycle Detection", "topological-sort", "Return True if a directed graph has a cycle using three states.", "def has_cycle(graph):\n    pass", simpleTests("has_cycle", ["has_cycle({0:[1],1:[2],2:[]}) is False", "has_cycle({0:[1],1:[0]}) is True"]), "def has_cycle(graph):\n    state = {}\n    def visit(node):\n        if state.get(node) == 1:\n            return True\n        if state.get(node) == 2:\n            return False\n        state[node] = 1\n        for nei in graph.get(node, []):\n            if visit(nei):\n                return True\n        state[node] = 2\n        return False\n    return any(visit(node) for node in graph)", "Three-state DFS separates visiting nodes from fully processed nodes.", 160),
  makeDrill("heap-top-k", "Maintain Top K", "heap", "Return the k largest values in ascending order using a min heap.", "import heapq\n\ndef top_k(nums, k):\n    pass", simpleTests("top_k", ["top_k([5,1,9,3,7], 3) == [5,7,9]", "top_k([2,1], 5) == [1,2]"]), "import heapq\n\ndef top_k(nums, k):\n    heap = []\n    for num in nums:\n        heapq.heappush(heap, num)\n        if len(heap) > k:\n            heapq.heappop(heap)\n    return sorted(heap)", "Keep a min heap capped at k so the smallest retained value is easy to discard.", 150),
  makeDrill("heap-kth-largest", "Kth Largest", "heap", "Return the kth largest value.", "import heapq\n\ndef kth_largest(nums, k):\n    pass", simpleTests("kth_largest", ["kth_largest([3,2,1,5,6,4], 2) == 5", "kth_largest([1], 1) == 1"]), "import heapq\n\ndef kth_largest(nums, k):\n    heap = []\n    for num in nums:\n        heapq.heappush(heap, num)\n        if len(heap) > k:\n            heapq.heappop(heap)\n    return heap[0]", "After retaining k largest values, the heap root is kth largest.", 130),
  makeDrill("heap-tuples", "Min-Heap Tuples", "heap", "Merge k sorted lists into one sorted list using heap tuples.", "import heapq\n\ndef merge_sorted_lists(lists):\n    pass", simpleTests("merge_sorted_lists", ["merge_sorted_lists([[1,4],[1,3],[2]]) == [1,1,2,3,4]", "merge_sorted_lists([]) == []"]), "import heapq\n\ndef merge_sorted_lists(lists):\n    heap = []\n    for i, values in enumerate(lists):\n        if values:\n            heapq.heappush(heap, (values[0], i, 0))\n    out = []\n    while heap:\n        value, list_i, item_i = heapq.heappop(heap)\n        out.append(value)\n        next_i = item_i + 1\n        if next_i < len(lists[list_i]):\n            heapq.heappush(heap, (lists[list_i][next_i], list_i, next_i))\n    return out", "Tuples let heapq sort by priority while carrying source metadata.", 180),
  makeDrill("backtracking-subsets", "Generate Subsets", "backtracking", "Return all subsets of nums.", "def subsets(nums):\n    pass", simpleTests("subsets", ["sorted(subsets([1,2])) == [[], [1], [1,2], [2]]", "subsets([]) == [[]]"]), "def subsets(nums):\n    out = []\n    path = []\n    def backtrack(i):\n        if i == len(nums):\n            out.append(path.copy())\n            return\n        backtrack(i + 1)\n        path.append(nums[i])\n        backtrack(i + 1)\n        path.pop()\n    backtrack(0)\n    return out", "Backtracking is choose, recurse, undo.", 180),
  makeDrill("backtracking-permutations", "Generate Permutations", "backtracking", "Return all permutations of nums.", "def permutations(nums):\n    pass", simpleTests("permutations", ["sorted(permutations([1,2])) == [[1,2],[2,1]]", "permutations([]) == [[]]"]), "def permutations(nums):\n    out = []\n    used = [False] * len(nums)\n    path = []\n    def backtrack():\n        if len(path) == len(nums):\n            out.append(path.copy())\n            return\n        for i, num in enumerate(nums):\n            if used[i]:\n                continue\n            used[i] = True\n            path.append(num)\n            backtrack()\n            path.pop()\n            used[i] = False\n    backtrack()\n    return out", "The used array prevents reusing the same element in one permutation.", 180),
  makeDrill("backtracking-combinations", "Combination Skeleton", "backtracking", "Return all size-k combinations from numbers 1..n.", "def combine(n, k):\n    pass", simpleTests("combine", ["combine(3, 2) == [[1,2],[1,3],[2,3]]", "combine(1, 1) == [[1]]"]), "def combine(n, k):\n    out = []\n    path = []\n    def backtrack(start):\n        if len(path) == k:\n            out.append(path.copy())\n            return\n        for value in range(start, n + 1):\n            path.append(value)\n            backtrack(value + 1)\n            path.pop()\n    backtrack(1)\n    return out", "Combination recursion advances the start index after each choice.", 180),
  makeDrill("stack-valid-parens", "Valid Parentheses", "stack", "Return True if brackets are balanced.", "def is_valid(s):\n    pass", simpleTests("is_valid", ["is_valid('([])') is True", "is_valid('([)]') is False", "is_valid('') is True"]), "def is_valid(s):\n    pairs = {')': '(', ']': '[', '}': '{'}\n    stack = []\n    for ch in s:\n        if ch in pairs.values():\n            stack.append(ch)\n        elif not stack or stack.pop() != pairs[ch]:\n            return False\n    return not stack", "Stacks model the most recent unmatched opener.", 100),
  makeDrill("intervals-merge", "Merge Intervals", "intervals", "Merge overlapping intervals.", "def merge_intervals(intervals):\n    pass", simpleTests("merge_intervals", ["merge_intervals([[1,3],[2,6],[8,10]]) == [[1,6],[8,10]]", "merge_intervals([]) == []"]), "def merge_intervals(intervals):\n    intervals.sort(key=lambda x: x[0])\n    merged = []\n    for start, end in intervals:\n        if not merged or start > merged[-1][1]:\n            merged.append([start, end])\n        else:\n            merged[-1][1] = max(merged[-1][1], end)\n    return merged", "Sort by start, then compare each interval to the last merged interval.", 130),
  makeDrill("prefix-sum", "Prefix Sum Range", "prefix-sum", "Build prefix sums and answer inclusive range sum queries.", "def range_sums(nums, queries):\n    pass", simpleTests("range_sums", ["range_sums([1,2,3,4], [(0,1),(1,3)]) == [3,9]", "range_sums([], []) == []"]), "def range_sums(nums, queries):\n    prefix = [0]\n    for num in nums:\n        prefix.append(prefix[-1] + num)\n    return [prefix[right + 1] - prefix[left] for left, right in queries]", "Prefix sums convert range sums into two array reads.", 100),
  makeDrill("monotonic-next-greater", "Next Greater Element", "monotonic-stack", "Return the next greater value for each number, or -1.", "def next_greater(nums):\n    pass", simpleTests("next_greater", ["next_greater([2,1,3]) == [3,3,-1]", "next_greater([3,2,1]) == [-1,-1,-1]"]), "def next_greater(nums):\n    out = [-1] * len(nums)\n    stack = []\n    for i, num in enumerate(nums):\n        while stack and nums[stack[-1]] < num:\n            out[stack.pop()] = num\n        stack.append(i)\n    return out", "A monotonic stack stores unresolved indexes.", 150),
  makeDrill("union-find-basic", "Union Find", "union-find", "Return the number of connected components after unioning edges.", "def count_components(n, edges):\n    pass", simpleTests("count_components", ["count_components(5, [(0,1),(1,2),(3,4)]) == 2", "count_components(3, []) == 3"]), "def count_components(n, edges):\n    parent = list(range(n))\n    def find(x):\n        while parent[x] != x:\n            parent[x] = parent[parent[x]]\n            x = parent[x]\n        return x\n    count = n\n    for a, b in edges:\n        ra, rb = find(a), find(b)\n        if ra != rb:\n            parent[rb] = ra\n            count -= 1\n    return count", "Union Find tracks components with representative parents.", 180),
  makeDrill("dp-climb", "Climbing Stairs", "dp", "Return the number of ways to climb n stairs taking 1 or 2 steps.", "def climb_stairs(n):\n    pass", simpleTests("climb_stairs", ["climb_stairs(1) == 1", "climb_stairs(3) == 3", "climb_stairs(5) == 8"]), "def climb_stairs(n):\n    if n <= 2:\n        return n\n    prev, cur = 1, 2\n    for _ in range(3, n + 1):\n        prev, cur = cur, prev + cur\n    return cur", "Many 1D DP drills are just previous states plus a transition.", 120),
  makeDrill("greedy-activity", "Greedy Intervals", "greedy", "Return the maximum number of non-overlapping intervals.", "def max_non_overlapping(intervals):\n    pass", simpleTests("max_non_overlapping", ["max_non_overlapping([[1,3],[2,4],[4,5]]) == 2", "max_non_overlapping([]) == 0"]), "def max_non_overlapping(intervals):\n    intervals.sort(key=lambda x: x[1])\n    count = 0\n    end = float('-inf')\n    for start, stop in intervals:\n        if start >= end:\n            count += 1\n            end = stop\n    return count", "Choosing the earliest finishing interval leaves maximum room for later choices.", 120),
];

export function getDrill(id: string) {
  return drills.find((drill) => drill.id === id);
}
